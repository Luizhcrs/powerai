name: CI & Cross-Platform Verification

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

jobs:
  test-unix:
    name: Test Unix (macOS & Linux)
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Ensure Dependencies
        run: |
          if [ "$RUNNER_OS" == "Linux" ]; then
            sudo apt-get update -qq && sudo apt-get install -y -qq jq curl
          fi

      - name: Run Test Suite Harness
        run: |
          chmod +x tests/test_harness.sh
          ./tests/test_harness.sh

      - name: Verify POSIX Shell Syntax
        run: |
          bash -n powerai.sh
          bash -n install.sh
          bash -n uninstall.sh

  test-windows:
    name: Test Windows (PowerShell)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Test PowerShell Module Syntax & Manifest
        shell: pwsh
        run: |
          Test-ModuleManifest -Path "src/PowerAI/PowerAI.psd1" -ErrorAction Stop
          Import-Module "./src/PowerAI/PowerAI.psm1" -Force -ErrorAction Stop
          Write-Host "PowerAI PowerShell module verified and imported successfully!"
